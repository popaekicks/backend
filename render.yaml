services:
  # Backend Web Service
  - type: web
    name: task-manager-backend
    plan: free
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: task-manager-db
          property: connectionString

  # Frontend Static Site
  - type: web
    name: task-manager-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      # Rewrite /api requests to the backend service
      # Using the unique backend URL provided by Render
      - type: rewrite
        source: /api/*
        destination: https://task-manager-backend-jctd.onrender.com/api/*
      # Ensure React Router works on page refresh
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: task-manager-db
    plan: free
